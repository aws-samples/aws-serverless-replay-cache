branches:
    only:
    - hex-arc
    
langugage: node_js

node_js: "12"

before_script:
    - npm install -g mocha
    
jobs:
    include:
        script:
            - name: 'Origin Request'
            - cd origin-request && npm test
        script:
            - name: 'Origin Response'
            - cd origin-response && npm test
    
